---
logging:
  level: DEBUG

management:
  context-path: /admin    

pidfile: /var/vcap/sys/run/broker/broker.pid

security:
	user:
		name: <%=broker.user%>
		password: <%=broker.password%>

spring:
  main:
    show-banner: true


    
cassandra:
	contactpoints: <% seeds = link('seeds') %><% seeds.instances.each do |instance| %><%= instance.address %>,<% end %>

spring:
	data:
		cassandra:
			password: <%=properties.cassandra_seed.admin_password%>	
	